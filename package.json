{
  "name": "newtons-fractals",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "yarn build && gh-pages --dist ./dist"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "ramda": "^0.27.1",
    "vite": "^2.6.4"
  }
}
