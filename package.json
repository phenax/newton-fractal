{
  "name": "newtons-fractals",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "yarn build && gh-pages --dist ./dist"
  },
  "dependencies": {
    "glslCanvas": "^0.1.7",
    "ramda": "^0.27.1"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "vite": "^2.6.4",
    "vite-plugin-plain-text": "^1.1.4"
  }
}
